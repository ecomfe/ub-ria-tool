/**
 * ${project.alias}
 *
 *
 * @ignore
 * @file 详情页Action基类
 * @author ${developer.name}(${developer.email})
 */
define(
    function (require) {
        return require('ub-ria/mvc/DetailAction');
    }
);
