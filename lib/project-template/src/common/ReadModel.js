 /**
 * ${project.alias}
 *
 *
 * @file 只读页基类Model
 * @class ReadModel
 * @extends ub-ria.ReadModel
 * @author ${developer.name}(${developer.email})
 */
define(
    function (require) {
        return require('ub-ria/mvc/ReadModel');
    }
);
