/**
 * ${project.alias}
 *
 *
 * @file 详情页视图基类
 * @class DetailView
 * @extends ub-ria.mvc.DetailView
 * @author ${developer.name}(${developer.email})
 */
define(
    function (require) {
        return require('ub-ria/mvc/DetailView');
    }
);
