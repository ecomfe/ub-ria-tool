 /**
 * ${project.alias}
 *
 *
 * @file 只读页基类Action
 * @class ReadAction
 * @extends ub-ria.ReadAction
 * @author ${developer.name}(${developer.email})
 */
define(
    function (require) {
        return require('ub-ria/mvc/ReadAction');
    }
);
